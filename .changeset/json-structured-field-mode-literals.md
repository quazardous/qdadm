---
"@quazardous/qdadm": patch
---

JsonStructuredField.jsonMode now accepts string literals ('tree' | 'text' | 'table') like its VanillaJsonEditor sibling — consumers no longer need to import the vanilla-jsoneditor Mode enum (#1280, mirror of #1253).
