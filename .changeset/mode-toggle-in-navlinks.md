---
"@quazardous/qdadm": patch
---

The breadcrumb View↔Edit toggle now renders in the right-side navlinks block (with the Details/child links) instead of trailing the breadcrumb, as a plain text link — no default icons (#1341 follow-up).
