---
"@quazardous/qdadm": minor
---

Per-entity punchline: new optional `description` on `EntityManager` — a one-liner explaining what the entity is, rendered as a muted subtitle under the list page title. `useListPage` picks it up automatically; override per page with the `subtitle` option (`null` suppresses it).
